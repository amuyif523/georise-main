# Baseline Results

Store baseline outputs (Artillery/Locust summaries, p95/p99 from `/api/admin/metrics`) here.

Suggested filename pattern:
`YYYY-MM-DD_env_db10_redis_default.json`

Include notes for:

- Postgres/Redis config (max connections, cache settings)
- Incident volume/seed data
- AI service model version
